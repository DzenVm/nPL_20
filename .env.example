# Opcjonalne. Bez tej zmiennej gtag.js w ogóle się nie ładuje. Ze zmienną —
# ładuje się zawsze, ale w trybie zgodnym z Google Consent Mode: dopóki
# odwiedzający nie zaakceptuje cookies reklamowych w banerze, sygnały zgody
# są ustawione na "denied" i skrypt nie zapisuje żadnych plików cookie
# reklamowych. Patrz src/components/GoogleTag.tsx i src/components/ConsentDefaults.tsx.
NEXT_PUBLIC_GOOGLE_ADS_ID=
